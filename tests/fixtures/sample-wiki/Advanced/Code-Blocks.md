# Code Blocks

Fenced code blocks are preserved as-is by Pandoc.

```typescript
function greet(name: string): string {
  return `Hello, ${name}!`;
}
```

Return to [[Getting-Started/Overview]] for setup steps.
